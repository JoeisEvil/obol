{
  "name": "ledger-core",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Multi-tenant MCP server for LEDGER — Company Registry + Stripe tools (mock-first)",
  "scripts": {
    "start": "bun index.ts",
    "dev": "bun --watch index.ts",
    "dry-run": "bun index.ts --dry-run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "qs": "^6.15.2",
    "stripe": "^17.5.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "bun-types": "^1.3.10"
  }
}
